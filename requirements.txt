# Legacy reference only. Use UV: uv sync --all-extras
# To regenerate from lockfile: uv export --format requirements-txt > requirements.txt
#
# Mimir RF Scanner — Python Dependencies
#
# System packages (install via package manager, NOT pip):
#   Fedora:        sudo dnf install hackrf SoapySDR python3-SoapySDR
#                  NOTE: SoapySDR-module-hackrf does NOT exist in dnf repos.
#                  Build from source: https://github.com/pothosware/SoapyHackRF
#   Ubuntu/Debian: sudo apt-get install hackrf soapysdr-module-hackrf python3-soapysdr
#
# Python packages (install via pip):

numpy>=1.26.0
pytest>=8.0.0
PyYAML>=6.0
requests>=2.31.0
chromadb>=0.5.0
flask>=3.0.0
flask-socketio>=5.3.0
python-engineio>=4.8.0
python-socketio[client]>=5.11.0
huggingface-hub>=0.24.0